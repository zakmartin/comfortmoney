# Comfort Money — konfigurace serveru
PORT=3000

# Doručení leadů: "webhook" (default) nebo "smtp"
LEAD_DELIVERY=webhook

# --- webhook ---
FORM_API_URL=

# --- smtp (LEAD_DELIVERY=smtp) ---
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
LEAD_EMAIL_TO=
